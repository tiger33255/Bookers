<h1>Editing Book</h1>

	<%= form_for(@book) do |f| %>
		<p>Title</p>
		<%= f.text_field :title %>

		<p>Body</p>
		<%= f.text_area :body %>
		<%= f.submit 'Update Book' %>
	<% end %>

<%= link_to "Edit", edit_book_path(@book) %>|<%= link_to "Back" , new_book_path %>